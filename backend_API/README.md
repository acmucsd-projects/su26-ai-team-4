# Building Damage Classifier for Local use and Server Upload
If you plan on having everything run on your own computer alone, then use the files contained in `backend_API/local`. Otherwise, if you plan on uploading or using a published URL, reference `backend_API/server`.

All instructions corresponding to each method are in the folders
